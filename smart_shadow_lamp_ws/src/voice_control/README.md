# voice_control

ROS2 package skeleton for wake word and fixed-command speech control.

Current speech pipeline development remains in `../../voice_control/`. This package is the future integration target for publishing `shadow_lamp_interfaces/VoiceCommand` on ROS2.
