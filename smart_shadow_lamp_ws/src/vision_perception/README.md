# vision_perception

ROS2 package skeleton for the camera and hand-shadow perception pipeline.

Current algorithm development remains in `../../camera/`. This package is the future integration target for publishing `shadow_lamp_interfaces/VisionState` on ROS2.
